<% @places.each do |place| %>
  <div class="booyah-box col-10 offset-1">
    <div class="row">
    <div class="col-6"><h1><%= link_to place.name, place_path(place) %></h1>
    <i><%= place.address %></i><br /><br />
    <p><%= place.description %></p><br />

    <% if place.user.present? %>
      <small><%= place.user.email %></small>
    <% end %>
    </div>
    <div class="col-4">
      <% if place.photos.present? %>
    <p><%= image_tag place.photos.first.picture, class:"indexPhoto float-right" %></p><br />
    <% end %>

    </div>
    </div>
    
<% if place.comments.present? %>
  <div class="col-12">
    <blockquote>
      <% if place.comments.last.humanized_rating.present? %>
        <b><%= place.comments.last.humanized_rating %></b> -
        <% end %>
        <%= place.comments.last.message %>
      <small><%= place.comments.last.user.email %></small>
    </blockquote>
  </div>
<% end %>
  </div>

<% end %>

<div class="flickr_pagination pagination">
   <%= will_paginate @places, renderer: BootstrapPagination::Rails %>
 </div>

 